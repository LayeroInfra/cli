// SSR Next.js config in .cjs format. Note: even with the literal
// `output: 'export'` in this comment, the detector must NOT classify
// this file as static-export — comments are stripped before regex.
module.exports = {
  reactStrictMode: true,
};
